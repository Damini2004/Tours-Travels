
import HeroSection from "@/components/hero/HeroSection";
import Image from "next/image";
import { Button } from "@/components/ui/button";
import Link from "next/link";

export default function HomePage() {
  return (
    <>
      <HeroSection />
      {/* 
        The "Why You Should Book With Us" and "Discover Your Ideal Escape" sections 
        have been removed as per your request.
        The PlacesGallery component, which is part of HeroSection, now handles 
        the "Today's Top Exclusive Offers" and the "Browse all offers" grid.
      */}
    </>
  );
}
